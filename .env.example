# Optional — email verification via Resend
# If not set, verification codes are logged to the serverless function logs
RESEND_API_KEY=re_xxxxxxxxx
EMAIL_FROM=ViperTech <onboarding@your-domain.com>

# Optional — restrict CORS origin (defaults to *)
# WEB_ORIGIN=https://your-vipertech.vercel.app
